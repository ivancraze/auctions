# Cargo Auctions

SPA для просмотра грузовых аукционов, истории ставок и установки своей ставки. Источником истины для API служит `openapi.auctions.v0.json`; локальный backend заменён stateful-моками MSW.

## Запуск

Требования: Node.js 26.5+ и npm 11.17+. Версии зафиксированы в `.nvmrc`, `.node-version` и `package.json`.

```bash
npm ci
npm run dev
```

`npm ci` устанавливает зависимости строго по `package-lock.json` и рекомендуется для воспроизводимой проверки задания. Для обычной локальной разработки также можно использовать `npm install`.

После запуска Vite выведет локальный адрес приложения. В development-режиме MSW перехватывает запросы к `/api/v1`, поэтому отдельный backend не требуется. Ответ списка намеренно задерживается на 700 мс: это позволяет проверить loader при применении и сбросе фильтров.

Основные команды:

```bash
npm test           # тесты Vitest
npm run typecheck  # проверка TypeScript
npm run lint       # ESLint и проверка FSD через Steiger
npm run lint:fsd   # отдельная проверка архитектурных правил FSD
npm run format     # форматирование Prettier
npm run build      # production-сборка
npm run test:coverage # тесты и отчёт о покрытии в терминале и coverage/index.html
npm run generate:api # повторная генерация TypeScript-типов и Zod-схем
npm run check:commit # проверки, выполняемые перед commit
npm run check:push   # проверки, выполняемые перед push
```

## Реализовано

- список аукционов по 6 карточек на страницу с пагинацией, URL-фильтрами, skeleton, empty и error states;
- безопасный разбор search params через Zod и восстановление значений по умолчанию;
- prefetch детальной страницы по intent средствами TanStack Router;
- адаптивные карточки со статусом, маршрутом, параметрами груза, ценой и доступным действием;
- детальная страница с организатором, контактами, точками маршрута, грузом, оплатой и параметрами торгов;
- история ставок с пагинацией, empty state и учётом `hide_bets_history`;
- форма ставки на React Hook Form + Zod с проверками `min`, `max` и `step`;
- обработка backend-ошибок `422`, success/error toast и инвалидация list/detail/bets queries;
- глобальный классовый Error Boundary;
- доступная мобильная панель фильтров с блокировкой фонового контента и управлением фокусом;
- сохранение выбранных фильтров и отфильтрованной выдачи при переходе на аукцион и возврате к списку;
- stateful MSW: успешная ставка меняет текущую/доступную цену, статус участника и историю ставок.

## Маршруты

- `/auctions` — список и фильтры;
- `/auctions/:auctionUuid` — детальная страница;
- `/auctions/:auctionUuid/bets` — история ставок;
- `/auctions/:auctionUuid/bet` — установка ставки.

## Архитектура

Код организован по слоям Feature-Sliced Design:

- `app` — провайдеры, маршрутизация, глобальные стили и обработка ошибок;
- `pages` — композиция страниц;
- `features` — фильтрация, просмотр истории и установка ставки;
- `entities` — API, модели, мапперы и UI аукциона;
- `shared` — API-клиент, сгенерированная схема, конфигурация и общие UI-элементы;
- `mocks` — MSW handlers, изменяемое mock-состояние и fixtures.

Для импортов из `src` используется алиас `@/*`. Относительные импорты сохранены только внутри одного модуля или slice.

Границы слоёв, публичные API slices и допустимые направления импортов дополнительно проверяются Steiger. Zustand используется только для состояния toast-уведомлений; server state хранится в TanStack Query, фильтры — в URL, а локальное UI-состояние — внутри компонентов и хуков.

Типы DTO генерируются из OpenAPI через `openapi-typescript`. Сетевой клиент дополнительно проверяет каждый успешный ответ и тело ошибки методом Zod `safeParse`. Несоответствие контракта превращается в `ApiContractError`, а штатная HTTP-ошибка — в `ApiError`.

ViewModel-мапперы преобразуют внешние DTO в стабильные модели, удобные компонентам: форматируют значения, задают безопасные fallback и вычисляют доступные действия. Благодаря этому UI не зависит напрямую от формы ответа backend, а изменения контракта локализуются на API-границе и в мапперах.

Сгенерированные TypeScript-типы и Zod-схемы не редактируются вручную. После изменения исходного OpenAPI-контракта их следует обновить командой `npm run generate:api`, а затем проверить typecheck и тесты.

Fixtures — это исходные тестовые данные для MSW: 35 аукционов и набор ставок с разными статусами, типами торгов и ограничениями. Они позволяют воспроизводимо проверять список, фильтрацию, пагинацию, скрытую/пустую историю, недоступную ставку и mutation без настоящего backend. Перед каждым тестом изменяемый mock-store восстанавливается из fixtures.

## UX и доступность

- фильтры хранятся в URL, поэтому сохраняются при навигации, возврате и передаче ссылки;
- загрузка, фоновое обновление и ошибки отображаются на основе состояний TanStack Query;
- переход на детальную страницу оформлен явным действием, а хлебные крошки показывают текущий уровень навигации;
- skip-link, семантические заголовки, подписи полей, управление фокусом и доступные диалоги поддерживают навигацию с клавиатуры и assistive technologies;
- анимации учитывают пользовательскую настройку `prefers-reduced-motion`.

## Проверка результата

Автоматически проверены:

- разбор URL search params и request builder;
- ViewModel-мапперы карточки, деталей и ставок;
- Zod-схема ставки, включая границы и шаг;
- соответствие API-ответов runtime-схемам;
- фильтрация, пагинация и изменение MSW-store после mutation;
- загрузка списка, error state и восстановление запроса;
- детали и переход к истории ставок;
- успешная ставка, локальная ошибка шага, backend `422` и закрытая форма;
- fallback глобального Error Boundary.

Текущий набор содержит 88 тестов в 15 файлах. Vitest контролирует минимальные пороги покрытия: 90% для statements, functions и lines, 75% для branches.

Финальный набор команд:

```bash
npm run typecheck
npm run lint
npm run format:check
npm test
npm run test:coverage
npm run build
```

После `npm install` скрипт `prepare` подключает Husky к локальному Git-репозиторию. Перед commit автоматически выполняются Prettier, ESLint, Steiger и Vitest; перед push — TypeScript typecheck и Vitest. Неуспешная проверка останавливает соответствующую Git-операцию.

## Допущения и ограничения

- MSW включается только в development. Production-сборка ожидает доступный backend на `/api/v1` либо соответствующий reverse proxy.
- Vite создаёт Brotli-копии подходящих production-ресурсов. Наличие `.br` в `dist` само по себе не включает сжатие на сервере: hosting или reverse proxy должен быть настроен на раздачу предварительно сжатых файлов.
- Состояние MSW хранится в памяти вкладки и сбрасывается после перезагрузки.
- Справочник городов локальный и намеренно небольшой.
- Полноценные E2E и визуальные regression-тесты не добавлены; основные пользовательские сценарии покрыты интеграционными тестами Vitest + Testing Library.

Подробности использования AI описаны в [AI_USAGE.md](./AI_USAGE.md).
